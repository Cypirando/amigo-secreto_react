import './Banner.css'

export const Banner = () => {
    // JSX
    return (
        <header className="banner">
            <img src="/images/bot-logo.jpg" alt="O banner principal da página do Botdesigner"/>
        </header>
    )
}